# MLX Inference Runtime

Small Apple Silicon inference runtime using MLX and MLX-LM as the model substrate.

The runtime owns decoding, cache policy, scheduling, timing boundaries, benchmark capture, and the CLI. MLX-LM is used for loading and reference behavior.
